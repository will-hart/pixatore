{
  "name": "@pixatore/ecs",
  "version": "0.1.0",
  "description": "A wrapper around @colyseus/ecs for use in pixatore",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "repository": "https://github.com/will-hart/pixatore",
  "author": "William Hart",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn jest",
    "build": "tsc -p tsconfig.json --resolveJsonModule",
    "build:core": "yarn build",
    "lint": "yarn run-s lint:tsc lint:eslint",
    "lint:tsc": "yarn tsc -p tsconfig.lint.json --noEmit",
    "lint:eslint": "yarn eslint",
    "build:watch": "tsc -w -p tsconfig.json --resolveJsonModule"
  },
  "dependencies": {
    "@colyseus/ecs": "0.4.0-alpha.4",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/jest": "26.0.10",
    "jest": "^26.4.0",
    "npm-run-all": "4.1.5",
    "ts-jest": "^26.2.0"
  }
}
