{
  "name": "@pixatore/app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:core": "echo 'Skipping'",
    "build:core:watch": "echo 'Skipping'",
    "lint": "echo 'Skipping'",
    "start": "yarn tauri dev",
    "build": "yarn tauri build",
    "test": "echo 'Skipping'"
  },
  "dependencies": {
    "tauri": "^0.11.1"
  }
}
