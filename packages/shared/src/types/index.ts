export { RoomOptions } from './roomOptions'
export { GameState } from './gameState'

export enum GameStatus {
  slotting = 'slotting',
  lobby = 'lobby',
  playing = 'playing',
  scoreScreen = 'ending',
}
